<div class="row">
  <div class="col-lg-12 mb-3">
    <h3 class="header-option pub-header">ログイン</h3>
  </div>
</div>

<div class="row">
  <div class="login-option">
    <p class="mb-5 login-text text-center">
      会員の方は<br>
      こちらからログイン
    </p>
    
    <!--バリデーションエラーメッセージ表示-->
    <%= render "public/shared/error_messages", resource: resource %>
    
    <%= form_with model: @user, url: session_path(resource_name) do |f| %>
      <div class="option-center">
        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "メールアドレス", class: "input-form" %>
        <%= f.password_field :password, autocomplete: "current-password", placeholder: "パスワード", class: "input-form" %>
        <%= f.submit "ログイン", class: "btn btn-option mt-4" %>
      </div>
    <% end %>
  </div>
</div>

<div class="row mt-3">
  <div class="col-lg-10 mx-auto">
    <h4 class="header-option pub-header">会員登録がお済みでない方</h4>
    <p>
      <%= link_to "こちら", new_registration_path(resource_name), class: "link-option" %>
      から新規登録を行ってください。
    <p>
  </div>
</div>