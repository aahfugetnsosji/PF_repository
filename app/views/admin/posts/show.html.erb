<div class="row mb-5">
  <div class="col-lg-12">
    <h1 class="header-option adm-header">投稿詳細</h1>
  </div>
</div>

<div class="row">
  <div class="col-lg-10 mx-auto">
    <h2 class="adm-header adm-show-border mb-5">
      投稿タイトル ：
      <%= @post.title %>
    </h2>
  </div>

  <div class="row mx-auto">
    <div class="col-lg-8 mx-auto">
      <div class="row">
        <div class="col-lg-4 mb-5">
          <h6 class="header-option adm-header">
            投稿ID ：
            <%= @post.id %>
          </h6>
        </div>

        <div class="col-lg-8 mb-5">
          <h6 class="header-option adm-header">
            投稿日時 ：
            <%= l @post.created_at %>
          </h6>
        </div>
      </div>

      <div class="row mx-auto">
          <h4 class="header-option adm-header mb-5">
            投稿者 ：
            <%= link_to @post.user.name, admin_user_path(@post.user_id), class: "adm-show-link-option" %>
          </h4>
      </div>

      <div class="row mb-5">
        <div class="col-lg-1"></div>

        <div class="col-lg-10">
          <div class="show-img-resize text-center">
            <%= image_tag @post.get_image %>
          </div>
        </div>

        <div class="col-lg-1"></div>
      </div>

      <h5 class="header-option adm-header">本文</h5>
      <div class="show-transparent-option text-normal">
        <%= safe_join(@post.body.split("\n"),tag(:br)) %>
      </div>

      <h5 class="header-option adm-header mt-5">タグ</h5>
      <div class="show-transparent-option area-font">
        <% if @post.post_tags.present? %>
          <% @post.post_tags.each do |pt| %>
            <span class="mr-3">
              <%= pt.tag.name %>
            </span>
          <% end %>
        <% else %>
          <div class="adm-gray-font">選択されていません</div>
        <% end %>
      </div>

      <h5 class="header-option adm-header mt-5">地方区分</h5>
      <div class="show-transparent-option area-font">
        <%= @post.region.region_name %>
      </div>

      <h5 class="header-option adm-header mt-5">都道府県</h5>
      <div class="show-transparent-option area-font">
        <% if @post.post_prefectures.present? %>
          <% @post.post_prefectures.each do |pp| %>
            <%= pp.prefecture.prefecture_name %>
          <% end %>
        <% else %>
          <div class="adm-gray-font">選択されていません</div>
        <% end %>
      </div>
    </div>
  </div>
</div>

<div class="comment-border mt-5 mb-4"></div>

<div class="row mx-auto">
  <div class="col-lg-9 mx-auto">
    <h5 class="header-option adm-header">
      コメント一覧
    </h5>
  </div>
</div>

<div class="row mx-auto">
  <div class="col-lg-8 mx-auto mb-5">
    <% if @post_comments.present? %>
      <% @post_comments.each do |post_comment| %>
        <div class="comment-option">
          <div class="comment-body">
            <%= safe_join(post_comment.comment.split("\n"),tag(:br)) %>
          </div>

          <div class="comment-name-date">
            <%= link_to post_comment.user.name, user_path(post_comment.user_id), class: "comment-name" %>
            <span class="comment-data">
              <%= l post_comment.created_at, format: :short %>
            </span>
          </div>
        </div>

        <div class="option-right">
          <%= link_to "削除", post_post_comment_path(post_comment.post, post_comment), method: :delete, "data-confirm" => "コメントを削除しますか？", class: "comment-delete" %>
        </div>
        <div class="comment-border"></div>
      <% end %>
    <% else %>
      <p class="basic-font ml-3">コメントはありません。</p>
    <% end %>
  </div>
</div>


<div class="mt-5 option-center">
  <%= link_to "投稿を削除する", admin_post_path(@post.id), method: :delete, "data-confirm" => "本当によろしいですか？", class: "btn danger-btn" %>
</div>